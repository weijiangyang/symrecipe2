{% extends 'base.html.twig' %}

{% block title %}Symrecipe - recettes{% endblock %}

{% block body %}
      <div class="container">
        
        <h1 class="mt-4 text-center">Recettes de la communauté</h1>
        <div class="d-flex justify-content-center flex-wrap mt-4">
          {% for recipe in recipes %}
             <div class="card border-primary mb-3 mx-3" style="max-width: 20rem;">
                <div class="card-header">Recette: No.{{ recipe.id }}</div>
                <div class="card-body">
                  <h4 class="card-title">{{ recipe.name }}</h4>
                  <p class="card-text">{{ recipe.description }}</p>
                </div>
              </div>
          {% endfor %}
        </div>
         
      </div>
    
    

{% endblock %}

