<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Navbar</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01"
            aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('home.index')}}">Accueil
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('ingredient.index')}}">Mes ingredients
                       
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('recipe.index')}}">Mes recettes
                       
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('recette.index.public')}}">Recettes communauté
                       
                    </a>
                </li>

                
                
            </ul>
            {% if app.user %}
                <div class="nav-item dropdown px-5">
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ app.user }}</a>
                        <div class="dropdown-menu">
                            
                            <a class="dropdown-item" href="{{ path('user.edit',{'id':app.user.id}) }}">Modifier le profile</a>
                            <a class="dropdown-item" href="{{  path('user.edit.password',{'id':app.user.id}) }}">Modifier le mot de passe</a>
                          
                            <div class="dropdown-divider"></div>
                            {% if 'ROLE_ADMIN' in app.user.roles%}
                            <a class="dropdown-item" href="{{ path('admin') }}">Administration</a>
                            {% endif %}
                            <a class="dropdown-item" href="{{ path('security.logout') }}">Deconnexion</a>
                        </div>
                </div>
            {% else %}
            <div class="d-flex">
                <a class="btn btn-primary" href="{{ path('security.login')}}">Connexion</a>
                <a class="btn btn-primary" href="{{ path('security.registration') }}">Inscription</a>
                       
                    
            </div>
                
            {% endif %}
            
    </div>
</nav>